[project]
name = "lamaquina"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "isort>=8.0.1",
    "pytest>=9.0.3",
    "ruff>=0.15.12",
]
